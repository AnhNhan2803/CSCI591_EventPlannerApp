import { CommServer, Publisher as BasePublisher } from '@ionic/discover';
export { computeBroadcastAddress } from '@ionic/discover';
export declare class Publisher extends BasePublisher {
    protected getInterfaces(): never[];
}
export declare function createPublisher(name: string, port: number, commPort: number): Promise<Publisher>;
export declare function createCommServer(id: string, port: number): Promise<CommServer>;
