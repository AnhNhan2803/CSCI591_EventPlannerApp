"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const Debug = require("debug");
const debug = Debug('ionic:lib:open');
async function openUrl(target, { app, encode: url = true } = {}) {
    const o = await Promise.resolve().then(() => require('open'));
    const p = await o(target, { wait: false, app, url });
    const e = (err) => debug('Error during open: %O', err);
    const n = p.on.bind(p);
    n('error', err => e(err));
}
exports.openUrl = openUrl;
